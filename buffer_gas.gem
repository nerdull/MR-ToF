; File: buffer_gas.gem
; Author: X. Chen
; Description: geometric definition of void space
; License: GNU GPLv3

# local a = _G.a
# local grid_unit = _G.grid_unit

# local n_a = math.ceil(a/grid_unit) + 2

pa_define($(n_a), $(n_a), $(n_a), planar, xyz, electrostatic,, $(grid_unit), surface=fractional)
