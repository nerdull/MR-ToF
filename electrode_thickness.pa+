--[[
File: electrode_thickness.pa+
Author: X. Chen
Description: proportional coefficients of electrode combinations for fast scaling potentials
License: GNU GPLv3
--]]

local n_ring = _G.n_ring

local map = {}

for i=1,n_ring do
    map[i] = (i%2==0) and -1 or 1
end

potential_array {
    scalable_electrodes = {
        [1] = map;
    }
}
